/* eslint-disable */
// Preload script for Ingentive Agent Dashboard.
// Reserved for future IPC between an embedded BrowserWindow and the main
// process. Today the app uses the system browser, so this file is intentionally
// minimal. Keep it in place so electron-builder/asar layout stays stable.

// No-op.
